{
  "private": true,
  "name": "latex-on-http-demo",
  "version": "0.0.1",
  "license": "MIT",
  "scripts": {
    "build": "NODE_OPTIONS=--openssl-legacy-provider preact build",
    "serve": "sirv build --cors --single",
    "dev": "NODE_OPTIONS=--openssl-legacy-provider preact watch",
    "lint": "eslint src"
  },
  "eslintConfig": {
    "extends": "preact",
    "ignorePatterns": [
      "build/"
    ]
  },
  "devDependencies": {
    "eslint": "^8",
    "eslint-config-preact": "^1.3",
    "identity-obj-proxy": "^3.0.0",
    "preact-cli": "^3.4",
    "sirv-cli": "^2"
  },
  "dependencies": {
    "@codemirror/lang-json": "^6",
    "@codemirror/language": "^6.6",
    "@codemirror/language-data": "^6.2",
    "@codemirror/legacy-modes": "^6.3",
    "@uiw/react-codemirror": "^4.19",
    "dot": "^1.1",
    "file-saver": "^2",
    "js-yaml": "^4",
    "preact": "^10.13",
    "preact-render-to-string": "^6",
    "preact-router": "^4.1"
  }
}
